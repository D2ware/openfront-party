{
  "name": "openfront-pre-lobby",
  "version": "0.1.0",
  "private": true,
  "description": "A public OpenFront match tracker with an optional party coordinator.",
  "scripts": {
    "start": "node server.cjs",
    "dev": "node --watch server.cjs",
    "test": "node --test",
    "build:pages": "node scripts/build-pages.cjs"
  },
  "dependencies": {
    "ws": "^8.18.3"
  }
}
